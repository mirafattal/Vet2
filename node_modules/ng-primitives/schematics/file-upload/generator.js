"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fileUploadGenerator = fileUploadGenerator;
/**
 * Copyright © 2024 Angular Primitives.
 * https://github.com/ng-primitives/ng-primitives
 *
 * This source code is licensed under the Apache 2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */
const devkit_1 = require("@nx/devkit");
const path = require("path");
const styles = `
:host {}

:host[data-dragover] {
}`;
const template = `<ng-content />`;
async function fileUploadGenerator(tree, options) {
    const { fileName } = (0, devkit_1.names)(options.name);
    (0, devkit_1.generateFiles)(tree, path.join(__dirname, 'files'), options.directory, {
        ...options,
        ...(0, devkit_1.names)(options.name),
        selector: `${options.prefix}-${fileName}`,
        styles,
        template,
    });
    if (options.inlineStyle) {
        tree.delete(path.join(options.directory, `${fileName}.component.${options.style}`));
    }
    if (options.inlineTemplate) {
        tree.delete(path.join(options.directory, `${fileName}.component.html`));
    }
    await (0, devkit_1.formatFiles)(tree);
}
exports.default = fileUploadGenerator;
//# sourceMappingURL=generator.js.map