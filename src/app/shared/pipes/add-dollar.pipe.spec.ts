import { AddDollarPipe } from './add-dollar.pipe';

describe('AddDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
